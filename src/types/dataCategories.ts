// TODO: Refactor this semantically and make it more expansive to TBX-Basic

export enum TermAdminType {
  Customer = "customerSubset",
  Project = "projectSubset",
  Admin = "source",
}

export enum TermDescripType {
  Context = "context",
}

export enum TermNoteType {
  PartOfSpeech = "partOfSpeech",
  ApprovalStatus = "administrativeStatus",
  Gender = "grammaticalGender",
  Region = "geographicalUsage",
}

export enum ConceptEntryDescripType {
  SubjectField = "subjectField",
}

export enum LangSecDescrip {
  Description = "definition",
}

export enum TermDescripAdminType {
  Source = "source"
}